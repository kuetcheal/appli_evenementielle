name: application_voyage
description: "A new Flutter project."
publish_to: 'none' # Remove this line si tu veux publier sur pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.6.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  shared_preferences: ^2.0.15
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/photos.jpg
    - assets/logo-event.png
    - assets/google.png
    - assets/cadeau.png
    - assets/apple.png
    - assets/bienvenu.png
    - assets/facebook.png
    - assets/concert.png
